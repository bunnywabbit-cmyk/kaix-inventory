# Only needed for production (Vercel) — leave unset for local dev, where
# Vite's dev server proxies /api to localhost:4000 (see vite.config.ts).
# Set this to your deployed Render backend's URL, including the /api suffix.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
